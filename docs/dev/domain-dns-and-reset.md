# Domain DNS and Tenant Reset Hardening

Tenant password reset fails closed when tenant context is missing. Domain DNS verification resolves the expected IP from `ARTSFOLIO_SERVER_PUBLIC_IP` or `SERVER_PUBLIC_IP`, with the current production IP as a last-resort fallback.

# End of file.
