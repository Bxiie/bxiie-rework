# Tenant help onboarding implementation

The platform help controller owns public help articles. Tenant signup queues lifecycle email bodies directly into `email_outbox`. The tenant-local setup checklist is rendered by `GettingStartedController`.

# End of file.
