# Dashboard signals

The platform dashboard shows tenant, pricing, sales, contact, and background-job signals from the live production tables.

The tenant dashboard shows tenant-scoped artwork, traffic, subscriber, message, order, and plan signals. If a dashboard section cannot be queried, the page reports a concise admin-visible query failure instead of implying that installed tables are missing.

# End of file.
