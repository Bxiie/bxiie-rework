# Tenant admin invites

Tenant admins can open **Admin > Users** and invite another tenant admin by email address. The invited user is attached to the tenant with admin permissions and receives an email queued through the platform email outbox.

If the user does not already have a local password, they should use the password reset flow from the tenant login page to set one before signing in.

# End of file.
