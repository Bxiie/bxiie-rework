# Administrative list tools

Platform Jobs, Email Outbox, Stats, Audit Log, and Tenants tables use the shared administrative table helper. Portfolio Sections and section artwork-order tables use the same helper.

Use the search field above a table to filter rows currently loaded on the page. Select a column heading to sort ascending or descending. Server-side pagination and filters remain authoritative where present.

<!-- End of file. -->
