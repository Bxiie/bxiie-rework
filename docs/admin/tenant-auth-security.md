# Tenant Password Reset and Logout Security

Tenant logout invalidates the current browser session on the server and clears session cookies.

Password reset links requested from a tenant site are only queued for active users of that tenant. The public response remains generic so account existence is not disclosed.

# End of file.
