# Platform admin URL

Use `https://artsfol.io/platform/admin` for platform administration.

If a platform-admin URL is opened on a tenant hostname, for example `https://bxiie.artsfol.io/platform/admin`, the application redirects to the canonical platform hostname.

# End of file.
