# Backup timestamp display

The Platform Admin Backups page keeps operational timestamps in UTC but displays
them in the signed-in administrator's configured IANA time zone.

This applies to hourly, weekly, and monthly backup results, detail-table
timestamps, and systemd timer next/last values.

<!-- End of file. -->
