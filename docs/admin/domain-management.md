# Domain Management

Platform admins can add custom domains by tenant ID or tenant slug. DNS verification uses `ARTSFOLIO_SERVER_PUBLIC_IP`, not the literal placeholder `SERVER_PUBLIC_IP`.

# End of file.
