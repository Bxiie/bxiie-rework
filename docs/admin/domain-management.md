# Domain Management

Tenant and platform admins can add, verify, and delete custom domains when the tenant plan allows custom domains. Platform admins can override plan checks when needed.

The default ArtsFolio subdomain does not count against custom-domain limits. `www` aliases share the apex domain allowance.

Deleting a tenant removes all tenant domain rows so the slug and hostnames can be reused.

# End of file.
