# Security-sensitive platform settings

Stripe secret values are never displayed after saving. Leave a secret field blank to preserve the configured value; enter a new value only to rotate it. Stripe webhooks return HTTP 503 until a webhook signing secret is configured. Repeated login and password-reset attempts are throttled.

<!-- End of file. -->
