# Signing in

Tenant administrators sign in from the tenant site's `/login` page. After a successful login, the browser is redirected to `/admin` for that tenant.

If login was attempted during an auth-cookie rollout and the browser is stuck returning to login, delete cookies for the tenant host and `artsfol.io`, then try again.

<!-- End of file. -->
