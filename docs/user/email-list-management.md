# Email list management

The Email Signups admin page lets you search, sort, import, export, annotate, unsubscribe, and delete email-list addresses.

CSV imports should include an `email` column. Optional columns are `name`, `source`, `notes`, and `consent_status`.

<!-- End of file. -->
