# Tenant Domain Security

ArtsFolio can automatically secure tenant subdomains after they are created.

Example:

```text
https://your-site.artsfol.io
```

A tenant subdomain must be registered in ArtsFolio before HTTPS certificates are issued.

<!-- End of file. -->
