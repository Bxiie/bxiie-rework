# Background Job Attempt History

## Current state

Job detail pages can show attempt history rows.

## Future requirements

```text
worker writes attempt start
worker writes attempt finish
capture exception class
capture duration
capture worker host/process id
show attempt detail page
```

<!-- End of file. -->
