import fs from 'node:fs';import path from 'node:path';import process from 'node:process';
const [scenesPath,audioRoot]=process.argv.slice(2);const apiKey=process.env.ELEVENLABS_API_KEY||'';const voiceId=process.env.ELEVENLABS_VOICE_ID||'';const modelId=process.env.ELEVENLABS_MODEL_ID||'eleven_multilingual_v2';const outputFormat=process.env.ELEVENLABS_OUTPUT_FORMAT||'mp3_44100_192';if(!apiKey||!voiceId)throw new Error('ElevenLabs credentials are missing.');const scenes=JSON.parse(fs.readFileSync(scenesPath,'utf8'));fs.mkdirSync(audioRoot,{recursive:true});const settings={stability:Number(process.env.ELEVENLABS_STABILITY||'0.52'),similarity_boost:Number(process.env.ELEVENLABS_SIMILARITY_BOOST||'0.78'),style:Number(process.env.ELEVENLABS_STYLE||'0.12'),use_speaker_boost:(process.env.ELEVENLABS_SPEAKER_BOOST||'true')==='true',speed:Number(process.env.ELEVENLABS_SPEED||'0.90')};let ordinal=0;for(const scene of scenes){for(const cue of scene.cues){ordinal++;const key=`${String(ordinal).padStart(3,'0')}-${scene.id}-${cue.id}`;const endpoint=`https://api.elevenlabs.io/v1/text-to-speech/${encodeURIComponent(voiceId)}?output_format=${encodeURIComponent(outputFormat)}`;const response=await fetch(endpoint,{method:'POST',headers:{Accept:'audio/mpeg','Content-Type':'application/json','xi-api-key':apiKey},body:JSON.stringify({text:cue.narration,model_id:modelId,voice_settings:settings})});if(!response.ok)throw new Error(`ElevenLabs HTTP ${response.status}: ${(await response.text()).slice(0,500)}`);fs.writeFileSync(path.join(audioRoot,`${key}.mp3`),Buffer.from(await response.arrayBuffer()));console.log(`[TTS] ${key}`);}}